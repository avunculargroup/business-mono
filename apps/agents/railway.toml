[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @platform/agents build"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
